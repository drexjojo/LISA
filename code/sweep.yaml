program: main.py
project: lisa
name: LR_GER_MUSIC
method: bayes
metric:
  name: Test Sentiment Loss
  goal: minimize
parameters:
  HIDDEN_SIZE_LSTM:
    min: 100
    max: 512

  HIDDEN_SIZE_LANG:
    min: 100
    max: 512
  
  HIDDEN_SIZE_SENT:
    min: 100
    max: 512

  LSTM_DROPOUT:
    min: 0.1
    max: 0.4
  
  EPOCH:
    min: 100
    max: 400

  BATCH_SIZE:
    min: 256
    max: 512

  LAMBDA:
    min: 0.1
    max: 0.9